# OlaForge - 智能安全沙箱系统架构设计

## 一、项目背景

本项目整合 SkillLite 和 CubeSandbox 的优点，创建一个更完美的 AI Agent 安全沙箱系统。

### 基础参考

| 项目 | 优点 | 缺点 |
|------|------|------|
| SkillLite | 自进化引擎、轻量级、零依赖 | 硬件隔离弱、集群能力弱 |
| CubeSandbox | 硬件级隔离、高性能、企业级 | 无智能化、部署复杂、运维要求高 |

---

## 二、核心设计理念

### 2.1 本地优先 (Local First)

为保证用户在本地高效简单易用，采用**渐进式复杂度**设计：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        OlaForge 使用模式                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   模式1: 即开即用 (开箱即用，零配置)                                          │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  pip install olaforge     # 一行安装                                │   │
│   │  olaforge start           # 一行启动                                │   │
│   │  olaforge chat            # 开始对话                                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   模式2: 自定义配置 (灵活配置)                                               │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  olaforge init            # 初始化                                  │   │
│   │  olaforge config          # 配置 LLM/API                           │   │
│   │  olaforge run             # 运行                                    │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   模式3: 高级开发 (企业级)                                                  │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  docker-compose up        # 集群部署                                │   │
│   │  或                                                               │   │
│   │  k8s deployment           # K8s 部署                               │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 本地模式架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      本地模式架构 (开发版)                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户终端                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                                                                     │   │
│   │   ┌─────────────┐                                                  │   │
│   │   │  CLI / GUI  │  ←── 一行命令或桌面应用                          │   │
│   │   └──────┬──────┘                                                  │   │
│   │          │                                                         │   │
│   │   ┌──────▼──────┐                                                  │   │
│   │   │   轻量 Agent │  ←── Rust 实现 (<10MB)                         │   │
│   │   │  (本地运行)  │                                                  │   │
│   │   └──────┬──────┘                                                  │   │
│   │          │                                                         │   │
│   │   ┌──────▼──────┐                                                  │   │
│   │   │  沙箱执行   │  ←── OS级隔离 (无 KVM 依赖)                      │   │
│   │   │ (bwrap等)   │     轻量: <50MB 内存                            │   │
│   │   └─────────────┘                                                  │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   本地运行，无需网络、容器、KVM                                              │
│   安装包 < 20MB，启动 < 1秒                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.3 零依赖安装

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          安装方式                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  方式1: pip (推荐)                                                          │
│  ─────────────────                                                          │
│  $ pip install olaforge                                                    │
│  $ olaforge --help                                                         │
│                                                                             │
│  方式2: 二进制下载                                                          │
│  ─────────────────                                                          │
│  $ curl -L https://olaforge.dev/install | sh                              │
│  $ olaforge --version                                                      │
│                                                                             │
│  方式3: Docker                                                              │
│  ─────────────────                                                          │
│  $ docker run -it olaforge/olaforge:latest                                 │
│                                                                             │
│  方式4: 源码编译                                                            │
│  ─────────────────                                                          │
│  $ git clone https://github.com/zyzheal/OlaForge                          │
│  $ cd OlaForge && cargo build --release                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.4 快速开始 (30秒)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          快速开始                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  步骤1: 安装 (5秒)                                                          │
│  ──────────────                                                             │
│  $ pip install olaforge                                                    │
│                                                                             │
│  步骤2: 配置 LLM (10秒)                                                     │
│  ──────────────────                                                         │
│  $ olaforge config --model claude-3-5-sonnet                              │
│  $ olaforge config --api-key sk-xxx                                        │
│                                                                             │
│  步骤3: 运行 (5秒)                                                          │
│  ─────────────                                                             │
│  $ olaforge chat "帮我写个快速排序"                                         │
│                                                                             │
│  或一条龙:                                                                  │
│  $ pip install olaforge && olaforge chat "hello"                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、系统架构 (完整版)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              OlaForge 系统架构                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         全球负载均衡                                  │   │
│  │                    (DNS/L7 负载 + 智能路由)                          │   │
│  └──────────────────────────┬──────────────────────────────────────────┘   │
│                              │                                               │
│                              ▼                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        API 网关层                                    │   │
│  │    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │   │
│  │    │  REST API   │  │  WebSocket  │  │   MCP SDK   │               │   │
│  │    └─────────────┘  └─────────────┘  └─────────────┘               │   │
│  └──────────────────────────┬──────────────────────────────────────────┘   │
│                              │                                               │
│                              ▼                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        编排调度层                                    │   │
│  │                    CubeMaster (集群编排)                            │   │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐               │   │
│  │  │  调度器      │ │  资源池管理   │ │  生命周期    │               │   │
│  │  │  Scheduler   │ │   Pool Mgr   │ │    Manager   │               │   │
│  │  └──────────────┘ └──────────────┘ └──────────────┘               │   │
│  └──────────────────────────┬──────────────────────────────────────────┘   │
│                              │                                               │
│              ┌───────────────┼───────────────┐                             │
│              ▼               ▼               ▼                              │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐             │
│  │   计算节点 1     │ │   计算节点 2     │ │   计算节点 N     │             │
│  │  (Worker Node)  │ │  (Worker Node)  │ │  (Worker Node)  │             │
│  │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │             │
│  │  │  Cubelet  │  │ │  │  Cubelet  │  │ │  │  Cubelet  │  │             │
│  │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │             │
│  │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │             │
│  │  │  KVM Pool │  │ │  │  KVM Pool │  │ │  │  KVM Pool │  │             │
│  │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │             │
│  └─────────────────┘ └─────────────────┘ └─────────────────┘             │
│                              │                                               │
└──────────────────────────────┼───────────────────────────────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        ▼                      ▼                      ▼
┌───────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   安全扫描层   │    │   智能进化层     │    │  可观测性层      │
│               │    │                 │    │                 │
│ Install-time │    │  进化引擎       │    │  监控           │
│ - Static     │    │  - Prompt      │    │  - Prometheus   │
│ - LLM 分析   │    │  - Memory      │    │  - Grafana      │
│ - 供应链     │    │  - Skill       │    │                 │
│               │    │                 │    │  日志           │
│ Pre-run      │    │  质量 gate     │    │  - Loki         │
│ - 双阶段确认  │    │  - 安全审查    │    │  - Elasticsearch│
│ - 完整性校验 │    │  - 性能评估    │    │                 │
│               │    │                 │    │  追踪           │
│ Runtime      │    │  回滚机制      │    │  - Jaeger       │
│ - OS 隔离    │    │  - 版本管理    │    │                 │
│ - eBPF 策略  │    │  - 快速恢复    │    │                 │
│ - 资源限制   │    │                 │    │                 │
└───────────────┘    └─────────────────┘    └─────────────────┘
```

---

## 三、核心组件

### 3.1 智能进化引擎

```
┌─────────────────────────────────────────────────────────────────┐
│                      智能进化引擎                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────┐ │
│  │  数据收集层     │───▶│  分析决策层     │───▶│  执行层     │ │
│  │                 │    │                 │    │             │ │
│  │ - 执行日志      │    │ - 模式识别      │    │ - 代码生成  │ │
│  │ - 成功/失败率   │    │ - 优化建议      │    │ - 测试验证  │ │
│  │ - 性能数据      │    │ - 风险评估      │    │ - 部署      │ │
│  │ - 用户反馈      │    │ - 进化决策      │    │ - 监控      │ │
│  └─────────────────┘    └─────────────────┘    └─────────────┘ │
│           │                      │                    │         │
│           └──────────────────────┴────────────────────┘         │
│                              │                                   │
│                              ▼                                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    质量 gate (必须通过)                       ││
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐        ││
│  │  │  安全扫描    │ │  单元测试    │ │  性能基准    │        ││
│  │  │  (L3 级别)   │ │  (全部通过)  │ │  (不劣化)    │        ││
│  │  └──────────────┘ └──────────────┘ └──────────────┘        ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 三层安全防护

```
┌─────────────────────────────────────────────────────────────────┐
│                      三层安全防护                                │
├─────────────────────────────────────────────────────────────────┤
│  Layer 1: 安装时扫描                                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  静态扫描          │  LLM 分析          │  供应链审计      │  │
│  │  正则规则匹配      │  风险评估          │  PyPI/OSV 检查   │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              │                                   │
│                              ▼                                   │
│  Layer 2: 执行前授权                                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  两阶段确认        │  完整性校验                         │  │
│  │  扫描→用户确认→执行 │  哈希+签名验证                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              │                                   │
│                              ▼                                   │
│  Layer 3: 运行时隔离                                             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  KVM MicroVM  │  OS级隔离(seccomp/namespace) │  eBPF网络  │  │
│  │  硬件虚拟化    │  系统调用限制               │  出站过滤   │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、技术栈

| 层级 | 技术 |
|------|------|
| 核心语言 | Rust, Go, Python |
| API 框架 | Actix-web (Rust), Gin (Go), FastAPI (Python) |
| 虚拟化 | KVM, Cloud Hypervisor, Firecracker |
| 网络安全 | eBPF, iptables, nftables |
| 编排 | Kubernetes, Docker |
| 存储 | PostgreSQL, Redis, etcd, Milvus |
| AI/ML | LangChain, Transformers, Ollama |

---

## 五、团队配备

### 核心团队 (17人)

| 组别 | 人数 | 角色 |
|------|------|------|
| 核心引擎研发 | 5 | Tech Lead + 2 Rust + 1 Go + 1 Python |
| 安全隔离研发 | 4 | Security Lead + 虚拟化 + eBPF + 安全测试 |
| 智能层研发 | 3 | AI Engineer + 算法 + 数据工程 |
| 平台研发 | 4 | Frontend Lead + 2 前端 + 1 全栈 |
| 运维保障 | 3 | DevOps Lead + SRE + 测试 |

---

## 六、性能指标

| 指标 | 目标值 |
|------|--------|
| 冷启动 | <100ms |
| 热启动 | <20ms |
| 内存开销 | <8MB/实例 |
| 安全评分 | 20/20 |
| 并发能力 | 1000+/节点 |

---

## 七、部署模式

### 模式1: 开发版 (单机)
- 适用：个人开发、快速原型

### 模式2: 企业版 (K8s集群)
- 适用：企业内部使用

### 模式3: 云服务版 (多Region)
- 适用：公共服务

---

## 八、版本规划

### v1.0 - 基础版
- KVM 隔离执行
- 基本安全扫描
- 简单资源调度

### v2.0 - 智能版
- 集成进化引擎
- LLM 辅助分析
- 自动优化

### v3.0 - 企业版
- 集群管理
- 多租户支持
- 完善监控运维

---

## 九、安全分级设计

针对评审中提出的安全分级需求，设计四级安全模式：

### 9.1 安全等级对比

| 等级 | 名称 | 隔离方式 | 适用场景 | 安全评分 |
|------|------|----------|----------|----------|
| L0 | 快速模式 | 无隔离 | 可信代码、快速测试 | 0/20 |
| L1 | 轻量级 | namespace + caps | 个人开发、无第三方 | 12/20 |
| L2 | 标准级 | bwrap/seccomp | 一般使用、外部代码 | 16/20 |
| L3 | 增强级 | KVM MicroVM | 敏感场景、高安全 | 20/20 |

### 9.2 各等级详细配置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        安全分级配置                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  L0 快速模式 (--security=fast)                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ - 无沙箱隔离                                                          │   │
│  │ - 直接执行 Python/Shell                                             │   │
│  │ - 用途：已完全信任的代码、可信环境                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  L1 轻量级 (--security=light)                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ - Linux namespace 隔离                                              │   │
│  │ - 能力降级 (CAP_DROP_ALL)                                           │   │
│  │ - 无网络限制                                                         │   │
│  │ - 文件访问限制: 仅 /home, /tmp, 项目目录                            │   │
│  │ - 内存限制: 2GB                                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  L2 标准级 (--security=standard) [默认]                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ - bubblewrap (bwrap) 隔离                                          │   │
│  │ - seccomp 系统调用过滤                                              │   │
│  │ - 网络 egress 白名单                                                │   │
│  │ - 文件访问限制: 仅为工作目录                                         │   │
│  │ - 禁止: 网络出站、进程派生、文件系统写入 /                          │   │
│  │ - 资源: CPU 2核, 内存 4GB, 60s 超时                                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  L3 增强级 (--security=enhanced)                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ - KVM MicroVM 硬件虚拟化                                            │   │
│  │ - 独立内核，无共享                                                   │   │
│  │ - 完全网络隔离 (可配置白名单)                                       │   │
│  │ - 资源: 可配置                                                       │   │
│  │ - 启用 eBPF 策略                                                    │   │
│  │ - 完整审计日志                                                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 9.3 自动选择策略

```python
# 自动安全等级选择
def auto_security_level(code_source: str, has_network: bool) -> str:
    if is_trusted_code(code_source):
        return "L0"  # 快速模式
    elif has_network and not is_verified():
        return "L3"  # 增强级
    else:
        return "L2"  # 默认标准级
```

---

## 十、性能优化方案

### 10.1 本地模式性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 安装后大小 | < 20MB | 精简二进制 |
| 首次启动 | < 2s | 包含模型加载 |
| 再次启动 | < 500ms | 热启动 |
| 内存占用 | < 100MB | 空闲状态 |
| 执行冷启动 | < 1s | 代码执行环境 |

### 10.2 性能优化策略

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        性能优化策略                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 预热机制                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  - 后台保活轻量 Agent                                                │   │
│  │  - 缓存已加载模块/模型                                               │   │
│  │  - 智能预判下一个可能的执行                                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  2. 延迟初始化                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  - 非核心模块按需加载                                                │   │
│  │  - 启动时只加载 CLI 框架                                             │   │
│  │  - LLM SDK 首次调用时初始化                                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  3. 多级缓存                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  L1: 内存缓存 (热点数据)                                            │   │
│  │  L2: 本地文件缓存 (Python 环境、依赖)                                 │   │
│  │  L3: 远程缓存 (可配置)                                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  4. 资源池 (企业版)                                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  - KVM 实例预热池                                                    │   │
│  │  - 快照克隆 (60ms 启动)                                             │   │
│  │  - 动态扩缩容                                                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 十一、MVP 验证计划

### 11.1 MVP 范围

| 阶段 | 时间 | 范围 |
|------|------|------|
| MVP v0.1 | 1周 | 本地 CLI + L0/L1 隔离 |
| MVP v0.2 | 2周 | L2 标准隔离 + 基本安全扫描 |
| MVP v0.3 | 3周 | 智能进化原型 + 性能优化 |
| MVP v1.0 | 4周 | 完整本地模式 + 文档 |

### 11.2 验证指标

```
MVP 验收标准:
├── 功能验收
│   ├── 安装成功率 100%
│   ├── CLI 命令可用率 100%
│   ├── 代码执行成功率 > 95%
│   └── 隔离功能正常工作
│
├── 性能验收
│   ├── 启动时间 < 2s
│   ├── 内存占用 < 100MB
│   └── 首次执行延迟 < 3s
│
└── 安全验收
    ├── L0-L3 分级正常切换
    ├── 危险命令被正确拦截
    └── 无逃逸风险
```

### 11.3 测试用例

| 用例 | 说明 |
|------|------|
| TC-001 | pip install olaforge 成功 |
| TC-002 | olaforge chat 正常启动 |
| TC-003 | 执行 "print('hello')" 成功 |
| TC-004 | rm -rf / 被拦截 |
| TC-005 | 网络请求被正确限制 |
| TC-006 | 切换 L0/L1/L2/L3 正常 |
| TC-007 | 内存超限被限制 |
| TC-008 | 执行超时被正确处理 |

---

## 十二、紧急改进项

### 12.1 已完成的改进

| 改进项 | 状态 |
|--------|------|
| 本地优先设计 | ✅ 已添加 |
| 安全分级设计 | ✅ 本节已完成 |
| 性能优化方案 | ✅ 本节已完成 |
| MVP 验证计划 | ✅ 本节已完成 |

### 12.2 后续改进计划

| 改进项 | 优先级 | 计划时间 |
|--------|--------|----------|
| 本地模式 MVP | P0 | 2周 |
| 安全分级实现 | P0 | 2周 |
| 性能基准测试 | P1 | 3周 |
| 完整文档 | P1 | 4周 |
| 企业版原型 | P2 | 8周 |

---

## 十三、架构评审结论

### 与 SkillLite 对比

| 维度 | SkillLite | OlaForge | 优势 |
|------|-----------|----------|------|
| 本地易用 | 好 | 更好 | 分级设计 |
| 安全性 | 好 | 更好 | 多层防护+KVM |
| 智能化 | 好 | 更好 | 完善进化流程 |
| 企业支持 | 弱 | 强 | K8s 部署 |

### 与 CubeSandbox 对比

| 维度 | CubeSandbox | OlaForge | 优势 |
|------|-------------|----------|------|
| 本地易用 | 差 | 优 | 零依赖 |
| 智能化 | 无 | 有 | 进化引擎 |
| 部署复杂度 | 高 | 低 | 渐进式 |
| 成本 | 高 | 低 | 本地免费 |

### 综合评价

✅ **优势明显**:
1. 本地易用性远超两者
2. 安全性继承并增强
3. 智能化填补 CubeSandbox 空白
4. 部署复杂度大幅降低

⚠️ **仍需验证**:
1. MVP 实际运行效果
2. 性能指标是否达标
3. 用户体验是否流畅

---

## 九、总结

OlaForge 整合了：

| 能力 | 来源 | 增强 |
|------|------|------|
| 硬件级隔离 | CubeSandbox | + 预热资源池 |
| 智能进化 | SkillLite | + 严格质量gate |
| 安全扫描 | SkillLite | + 多层防护 |
| 高性能 | CubeSandbox | + 智能调度 |
| 企业级 | 两者结合 | + 完善监控/运维 |

**核心价值**：
- AI Native (智能进化)
- Security First (多层安全)
- Enterprise Ready (企业级)

---

## 十四、CLI 命令设计

### 14.1 命令结构

```
olaforge <command> [options] [arguments]

Commands:
  chat         启动交互式对话
  run          单次执行代码
  config       配置管理
  init         初始化项目
  skill        Skill 管理
  status       查看状态
  logs         查看日志
  version      版本信息
  help         帮助信息
```

### 14.2 详细命令

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           CLI 命令详解                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  chat [options]                                                            │
│  ─────────────────                                                         │
│  交互式对话模式                                                            │
│                                                                             │
│  选项:                                                                     │
│    -m, --model <name>      指定模型 (默认: claude-3-5-sonnet)              │
│    -s, --security <level>  安全等级 L0-L3 (默认: L2)                       │
│    --no-sandbox           禁用沙箱 (仅 L0)                                  │
│    -v, --verbose          详细输出                                          │
│                                                                             │
│  示例:                                                                     │
│    $ olaforge chat                                                        │
│    $ olaforge chat -m gpt-4                                               │
│    $ olaforge chat --security L3                                          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  run <prompt> [options]                                                    │
│  ──────────────────                                                        │
│  单次执行模式                                                              │
│                                                                             │
│  选项:                                                                     │
│    -m, --model <name>      指定模型                                         │
│    -s, --security <level>  安全等级                                         │
│    -t, --timeout <sec>     超时时间 (默认: 60)                              │
│    -o, --output <path>     输出文件                                         │
│                                                                             │
│  示例:                                                                     │
│    $ olaforge run "print('hello')"                                        │
│    $ olaforge run "calculate fibonacci(10)" -t 30                         │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  config [action] [key] [value]                                             │
│  ───────────────────────────                                               │
│  配置管理                                                                  │
│                                                                             │
│  操作:                                                                     │
│    set <key> <value>    设置配置                                            │
│    get <key>            获取配置                                            │
│    list                 列出所有配置                                        │
│    reset                重置为默认                                          │
│    export               导出配置                                            │
│    import <path>        导入配置                                            │
│                                                                             │
│  示例:                                                                     │
│    $ olaforge config set api_key sk-xxx                                    │
│    $ olaforge config set model claude-3-5-sonnet                          │
│    $ olaforge config list                                                  │
│    $ olaforge config reset                                                 │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  skill [action] [options]                                                  │
│  ────────────────────────                                                  │
│  Skill 管理                                                                │
│                                                                             │
│  操作:                                                                     │
│    list                   列出已安装 skill                                  │
│    add <source>           添加 skill                                        │
│    remove <name>          移除 skill                                        │
│    info <name>            查看 skill 信息                                   │
│    enable <name>          启用 skill                                        │
│    disable <name>         禁用 skill                                        │
│                                                                             │
│  示例:                                                                     │
│    $ olaforge skill list                                                   │
│    $ olaforge skill add owner/repo                                         │
│    $ olaforge skill add ./my-skill                                         │
│    $ olaforge skill remove my-skill                                        │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  init [options]                                                            │
│  ─────────────                                                             │
│  初始化项目                                                                │
│                                                                             │
│  选项:                                                                     │
│    --force                 强制初始化                                        │
│    --template <name>      使用模板                                          │
│    --dir <path>           指定目录 (默认: 当前)                             │
│                                                                             │
│  示例:                                                                     │
│    $ olaforge init                                                         │
│    $ olaforge init --template python                                       │
│    $ olaforge init --dir ./my-project                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 十五、配置文件设计

### 15.1 配置文件位置

```
~/.olaforge/
├── config.yaml          # 主配置文件
├── models.yaml          # 模型配置
├── skills/              # 本地 skill 目录
├── cache/               # 缓存目录
├── logs/                # 日志目录
└── memory/              # 记忆存储

项目目录/
├── .olaforge/           # 项目级配置 (可选)
├── skills/              # 项目 skill
└── .gitignore
```

### 15.2 主配置文件格式

```yaml
# ~/.olaforge/config.yaml
version: "1.0"

# 模型配置
model:
  provider: "openai"              # openai/anthropic/local
  name: "claude-3-5-sonnet"       # 模型名称
  api_key: "${OLA_API_KEY}"       # 环境变量引用
  base_url: ""                    # 自定义 API 端点
  timeout: 60                     # 请求超时 (秒)
  max_retries: 3                  # 最大重试次数

# 安全配置
security:
  level: "L2"                     # L0/L1/L2/L3
  auto_level: true                # 自动选择安全等级
  network_allowed: false          # 是否允许网络
  file_write_allowed: false       # 是否允许写文件

# 执行配置
execution:
  timeout: 60                     # 执行超时 (秒)
  memory_limit: 4096              # 内存限制 (MB)
  cpu_limit: 2                    # CPU 核心数
  temp_dir: "/tmp/olaforge"      # 临时目录

# 进化配置
evolution:
  enabled: true                   # 是否启用进化
  auto_save: true                 # 自动保存
  feedback_threshold: 0.8         # 反馈阈值
  max_memory_items: 1000         # 最大记忆条目

# 日志配置
logging:
  level: "info"                   # debug/info/warn/error
  file: "~/.olaforge/logs/olaforge.log"
  max_size: 10                    # 单个日志文件大小 (MB)
  max_files: 5                    # 保留文件数

# 高级配置
advanced:
  cache_enabled: true             # 启用缓存
  preload_models: false           # 预加载模型
  parallel_execution: false       # 并行执行
  sandbox_engine: "bwrap"         # bwrap/firejail/none
```

### 15.3 环境变量

| 变量名 | 说明 | 必填 |
|--------|------|------|
| `OLA_API_KEY` | API Key | 是 |
| `OLA_BASE_URL` | 自定义 API 端点 | 否 |
| `OLA_MODEL` | 默认模型 | 否 |
| `OLA_SECURITY` | 默认安全等级 | 否 |
| `OLA_CONFIG_PATH` | 配置文件路径 | 否 |
| `OLA_NO_SANDBOX` | 禁用沙箱 | 否 |

---

## 十六、Skill 定义格式

### 16.1 Skill 目录结构

```
my-skill/
├── SKILL.md           # Skill 元数据 (必须)
├── scripts/           # 脚本目录
│   ├── main.py        # 主入口 (可选)
│   ├── handler.py     # 处理器 (可选)
│   └── utils.py       # 工具 (可选)
├── references/        # 参考文档 (可选)
│   └── api.md
├── assets/            # 资源文件 (可选)
│   └── icon.png
└── tests/             # 测试 (可选)
    └── test_skill.py
```

### 16.2 SKILL.md 格式

```yaml
---
name: my-skill
version: "1.0.0"
description: "我的自定义 Skill，用于处理特定任务"
author: your-name
license: MIT
compatibility: "olaforge >= 1.0.0"

# 依赖声明
dependencies:
  python:
    - requests>=2.28.0
    - numpy>=1.21.0
  system: []

# 输入参数
input:
  query:
    type: string
    required: true
    description: "用户查询"

  options:
    type: object
    required: false
    properties:
      mode:
        type: string
        enum: [fast, accurate]
        default: fast

# 输出格式
output:
  type: object
  properties:
    result:
      type: string
      description: "执行结果"
    confidence:
      type: number
      description: "置信度"

# 信任级别
trust:
  level: "medium"              # low/medium/high
  requires_confirmation: false
  can_network: false
  can_write_files: false

# 性能配置
performance:
  timeout: 30
  memory_limit: 512
  cpu_priority: normal
---

# 详细描述

这个 Skill 用于...

## 使用示例

```python
result = run_skill("my-skill", {"query": "xxx"})
```

## 注意事项

- 需要网络连接
- 处理时间可能较长
```

---

## 十七、SDK 设计

### 17.1 Python SDK

```python
# 安装
pip install olaforge

# 使用示例
from olaforge import OlaForge

# 初始化
client = OlaForge(
    api_key="sk-xxx",
    model="claude-3-5-sonnet",
    security="L2"
)

# 方式1: 对话
response = client.chat("帮我写个快速排序")
print(response.text)

# 方式2: 执行代码
result = client.run_code(
    "print(sum([1,2,3,4,5]))",
    language="python"
)
print(result.output)

# 方式3: 使用 Skill
result = client.run_skill(
    "my-skill",
    {"query": "xxx"}
)

# 方式4: 流式对话
for chunk in client.stream_chat("讲个故事"):
    print(chunk, end="")
```

### 17.2 REST API

```
Base URL: http://localhost:7860/api/v1

Endpoints:

POST /chat
  发送对话请求
  
  Request:
  {
    "message": "xxx",
    "model": "claude-3-5-sonnet",
    "security": "L2",
    "stream": false
  }
  
  Response:
  {
    "id": "msg_xxx",
    "text": "xxx",
    "usage": {...}
  }

POST /run
  执行代码
  
  Request:
  {
    "code": "print('hello')",
    "language": "python",
    "security": "L2"
  }

GET /status
  获取状态

GET /config
  获取配置

PUT /config
  更新配置

POST /skill
  执行 Skill
```

### 17.3 MCP 协议支持

```json
{
  "name": "olaforge",
  "version": "1.0.0",
  "capabilities": {
    "tools": true,
    "resources": true,
    "prompts": true
  },
  "tools": [
    {
      "name": "olaforge_run",
      "description": "Execute code in a secure sandbox",
      "inputSchema": {
        "type": "object",
        "properties": {
          "code": {"type": "string"},
          "language": {"type": "string", "enum": ["python", "javascript", "bash"]},
          "security": {"type": "string", "enum": ["L0", "L1", "L2", "L3"]}
        },
        "required": ["code"]
      }
    }
  ]
}
```

---

## 十八、错误处理

### 18.1 错误码体系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           错误码体系                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  格式: OLA-XXX-Y                                                          │
│                                                                             │
│  XXX: 模块代码                                                             │
│  Y:  错误类型 (1=参数, 2=执行, 3=安全, 4=系统)                              │
│                                                                             │
│  通用错误 (000)                                                            │
│  ───────────────                                                           │
│  OLA-000-1  参数错误                                                        │
│  OLA-000-2  执行超时                                                        │
│  OLA-000-3  权限不足                                                        │
│  OLA-000-4  系统错误                                                        │
│                                                                             │
│  配置模块 (100)                                                            │
│  ───────────────                                                           │
│  OLA-100-1  配置项不存在                                                    │
│  OLA-100-2  配置格式错误                                                    │
│  OLA-100-3  API Key 无效                                                    │
│                                                                             │
│  执行模块 (200)                                                            │
│  ───────────────                                                           │
│  OLA-200-1  代码执行超时                                                    │
│  OLA-200-2  内存超限                                                        │
│  OLA-200-3  CPU 超限                                                        │
│  OLA-200-4  进程崩溃                                                        │
│                                                                             │
│  安全模块 (300)                                                            │
│  ───────────────                                                           │
│  OLA-300-1  沙箱初始化失败                                                  │
│  OLA-300-2  权限被拒绝                                                      │
│  OLA-300-3  危险操作被拦截                                                  │
│  OLA-300-4  网络访问被拒绝                                                  │
│  OLA-300-5  文件访问被拒绝                                                  │
│                                                                             │
│  模型模块 (400)                                                            │
│  ───────────────                                                           │
│  OLA-400-1  模型加载失败                                                    │
│  OLA-400-2  API 请求失败                                                    │
│  OLA-400-3  模型响应错误                                                    │
│  OLA-400-4  Token 超限                                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 18.2 错误响应格式

```json
{
  "error": {
    "code": "OLA-300-3",
    "message": "危险操作被拦截",
    "detail": "命令 'rm -rf /' 被安全策略拦截",
    "recoverable": false,
    "suggestion": "请移除危险操作后重试"
  },
  "request_id": "req_xxx"
}
```

### 18.3 重试策略

```python
# 默认重试配置
retry_config = {
    "max_retries": 3,
    "backoff_factor": 2,        # 指数退避
    "initial_delay": 1,         # 初始延迟 (秒)
    "max_delay": 60,            # 最大延迟 (秒)
    "retry_on": [
        "OLA-000-4",            # 系统错误
        "OLA-400-2",            # API 请求失败
    ]
}

# 示例
@retry(max_retries=3, backoff=2)
def api_call():
    pass
```

---

## 十九、功能检查清单

### 19.1 核心功能

| 功能 | 状态 | 优先级 |
|------|------|--------|
| CLI 命令 chat | ✅ 已设计 | P0 |
| CLI 命令 run | ✅ 已设计 | P0 |
| CLI 命令 config | ✅ 已设计 | P0 |
| CLI 命令 init | ✅ 已设计 | P1 |
| CLI 命令 skill | ✅ 已设计 | P1 |
| 配置文件格式 | ✅ 已设计 | P0 |
| 环境变量支持 | ✅ 已设计 | P0 |
| Skill 定义格式 | ✅ 已设计 | P1 |
| Python SDK | ✅ 已设计 | P1 |
| REST API | ✅ 已设计 | P2 |
| MCP 协议 | ✅ 已设计 | P2 |
| 错误码体系 | ✅ 已设计 | P1 |
| 重试策略 | ✅ 已设计 | P1 |

### 19.2 后续完善

| 功能 | 说明 |
|------|------|
| Web UI | 桌面应用界面 |
| VSCode 插件 | IDE 集成 |
| API 详细规范 | OpenAPI 规范 |
| 测试用例 | 单元测试 + E2E |

---

## 二十、OlaForge 与 Claude Code 组合方案

### 20.1 组合架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     OlaForge + Claude Code 组合使用                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   用户                                                                   │
│     │                                                                    │
│     ▼                                                                    │
│   ┌──────────────────────────────────────────────────────────────────┐    │
│   │                   Claude Code CLI                               │    │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐                │    │
│   │  │  对话理解   │→│  工具决策   │→│  代码生成  │                │    │
│   │  └────────────┘  └────────────┘  └────────────┘                │    │
│   │        │              │              │                          │    │
│   │        └──────────────┴──────────────┘                          │    │
│   │                       │                                           │    │
│   └───────────────────────┼───────────────────────────────────────────┘    │
│                           │                                                │
│                           ▼                                                │
│   ┌──────────────────────────────────────────────────────────────────┐    │
│   │                   OlaForge 安全沙箱                              │    │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐                │    │
│   │  │ 安全扫描   │→│  隔离执行   │→│ 结果返回   │                │    │
│   │  └────────────┘  └────────────┘  └────────────┘                │    │
│   │                                                                 │    │
│   └──────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│   结果返回给 Claude Code → 显示给用户                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 20.2 组合模式

#### 模式1: OlaForge 作为 Claude Code 的安全执行器

```
用户: "帮我写个排序算法并执行"

Claude Code:
1. 理解需求，生成代码
2. 调用 BashTool 执行: olaforge run "排序代码"
3. OlaForge 在沙箱中执行代码
4. 返回结果给 Claude Code
5. Claude Code 展示结果
```

**优势**：
- 代码在隔离环境中执行，安全
- 可限制资源，防止滥用
- 审计执行记录

#### 模式2: OlaForge 为 Claude Code 提供 Skill

```
OlaForge Skill 可以被 Claude Code 调用：

1. 定义安全 Skill
2. Claude Code 通过 MCP 调用
3. OlaForge 执行并返回结果
```

#### 模式3: 本地开发组合

```
┌────────────────────────────────────────────────────────────────┐
│                       本地开发工作流                            │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  开发环境 (VSCode + Claude Code)                               │
│  ├── 智能编码 (Claude Code)                                    │
│  └── 工具集成 (OlaForge Skill)                                 │
│                                                                 │
│         │                                                       │
│         ▼                                                       │
│  测试环境 (OlaForge 沙箱)                                       │
│  ├── 单元测试                                                  │
│  ├── 安全扫描                                                  │
│  └── 代码审计                                                  │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

### 20.3 技术集成方案

#### 方式1: 通过 MCP 协议集成

```bash
# 1. 启动 OlaForge MCP 服务
olaforge mcp

# 2. 配置 Claude Code 使用 MCP
# 在 Claude Code 配置中添加:
```

```json
{
  "mcpServers": {
    "olaforge": {
      "command": "olaforge",
      "args": ["mcp"]
    }
  }
}
```

# 3. Claude Code 即可调用 OlaForge 工具

#### 方式2: 通过命令行集成

```python
# Claude Code 配置自定义工具
# config.yaml

tools:
  - name: "olaforge_run"
    command: "olaforge run {{code}}"
    sandbox: true
    timeout: 60
```

#### 方式3: 作为执行后端

```python
# 让 Claude Code 使用 OlaForge 作为代码执行后端

# 环境变量
export CLAUDE_CODE_EXEC_BACKEND="olaforge"
export OLAFORGE_SECURITY_LEVEL="L2"

# Claude Code 执行代码时会自动调用 OlaForge
```

### 20.4 组合配置示例

```yaml
# Claude Code 配置 claude-code.yaml
model: "claude-3-5-sonnet"
api_key: "${ANTHROPIC_API_KEY}"

# 添加 OlaForge 集成
execution:
  backend: "olaforge"           # 使用 OlaForge 执行
  security_level: "L2"          # 安全等级
  timeout: 60                   # 超时

# 可选: 配置 Skill
skills:
  - path: "~/.olaforge/skills"
  - name: "my-custom-skill"
```

### 20.5 使用场景

| 场景 | 组合方式 |
|------|----------|
| **安全代码执行** | Claude Code 生成 → OlaForge 执行 → 返回结果 |
| **受限环境开发** | 限制网络/文件 → OlaForge L2 隔离 |
| **代码审计** | 让 AI 审查代码 → 在沙箱中测试 |
| **学习环境** | 学生代码 → OlaForge 隔离执行 → 安全 |
| **自动化测试** | 测试用例 → OlaForge 运行 → 返回结果 |

### 20.6 安全收益

| 维度 | 单独 Claude Code | 组合 OlaForge |
|------|------------------|---------------|
| 代码执行 | 主机直接执行 | 沙箱隔离执行 |
| 资源限制 | 无限制 | 可配置限制 |
| 网络访问 | 无限制 | 可阻断 |
| 文件访问 | 无限制 | 可限制 |
| 审计记录 | 无 | 完整日志 |

---

## 二十一、系统方案评估与竞品对比

### 21.1 竞品生态图谱

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        AI 代码执行沙箱生态                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────┐      ┌───────────────────────┐                 │
│  │    商业产品           │      │    开源产品           │                 │
│  ├───────────────────────┤      ├───────────────────────┤                 │
│  │  • E2B               │      │  • SkillLite          │                 │
│  │  • CodeSandbox       │      │  • CubeSandbox        │                 │
│  │  • GitHub Codespaces │      │  • OlaForge (本项目)  │                 │
│  │  • Replit            │      │  • WebContainer API   │                 │
│  │  • Cursor            │      │  • Pyodide           │                 │
│  └───────────────────────┘      └───────────────────────┘                 │
│                                                                             │
│  ┌───────────────────────┐      ┌───────────────────────┐                 │
│  │    云服务             │      │    本地工具           │                 │
│  ├───────────────────────┤      ├───────────────────────┤                 │
│  │  • AWS CloudShell    │      │  • Docker            │                 │
│  │  • Azure Cloud Shell │      │  • Firejail          │                 │
│  │  • GCP Cloud Shell   │      │  • bubblewrap        │                 │
│  └───────────────────────┘      └───────────────────────┘                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 21.2 核心竞品详细对比

#### 21.2.1 E2B 对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              E2B 对比                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  维度          │  E2B                   │  OlaForge                 │     │
│  ──────────────┼────────────────────────┼───────────────────────────┤     │
│  定位          │ 云端沙箱服务           │ 本地优先 + 云端          │     │
│  隔离          │ MicroVM                │ bwrap/KVM               │     │
│  启动速度      │ ~200ms                 │ <50ms (本地)            │     │
│  成本          │ 付费订阅               │ 开源免费                │     │
│  本地支持      ❌ 无                    ✅ 零依赖                  │     │
│  智能化        ❌ 无                    ✅ 进化引擎               │     │
│  定制化        有限                    ✅ 完全开源               │     │
│                                                                             │
│  E2B 优势:                                                                   │
│  • 成熟稳定                                                                    │
│  • 无需维护                                                                    │
│  • 生态完善                                                                   │
│                                                                             │
│  OlaForge 优势:                                                             │
│  • 本地免费使用                                                              │
│  • 可自定义                                                                  │
│  • 智能进化能力                                                              │
│  • 更强安全控制                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 21.2.2 GitHub Codespaces 对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Codespaces 对比                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  维度          │  Codespaces          │  OlaForge                 │     │
│  ──────────────┼──────────────────────┼───────────────────────────┤     │
│  定位          │ 云端开发环境         │ 代码执行沙箱              │     │
│  启动          │ 30s+                 │ <1s (本地)               │     │
│  成本          │ 免费 60h/月          │ 免费无限                 │     │
│  资源          │ 2-32 核              │ 可配置                   │     │
│  离线          ❌ 无法                ✅ 支持                   │     │
│  集成          GitHub 深度           通用                      │     │
│                                                                             │
│  Codespaces 优势:                                                           │
│  • 完整开发环境                                                            │
│  • GitHub 深度集成                                                         │
│  • VS Code 完整支持                                                        │
│                                                                             │
│  OlaForge 优势:                                                            │
│  • 即开即用                                                               │
│  • 资源消耗低                                                             │
│  • 安全隔离更强                                                           │
│  • 成本为零                                                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 21.2.3 Docker/容器对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Docker 对比                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  维度          │  Docker                 │  OlaForge                 │     │
│  ──────────────┼─────────────────────────┼───────────────────────────┤     │
│  启动          │ 2-30s                   │ <1s (轻量模式)           │     │
│  资源          │ 100MB+                  │ <50MB                    │     │
│  隔离级别      │ 共享内核                │ 独立内核 (KVM)          │     │
│  安全性        │ 中                     │ 高                      │     │
│  易用性        │ 需要 Dockerfile        │ 即装即用                 │     │
│  场景          │ 完整环境               │ 代码执行                 │     │
│                                                                             │
│  Docker 优势:                                                             │
│  • 通用性强                                                               │
│  • 生态丰富                                                               │
│  • 可运行任意镜像                                                         │
│                                                                             │
│  OlaForge 优势:                                                           │
│  • 启动更快                                                               │
│  • 资源更少                                                               │
│  • 安全隔离更强 (可选 KVM)                                                │
│  • 专为 AI 代码执行优化                                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 21.2.4 Pyodide 对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Pyodide 对比                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  维度          │  Pyodide               │  OlaForge                 │     │
│  ──────────────┼────────────────────────┼───────────────────────────┤     │
│  语言          │ 仅 Python              │ 多语言                   │     │
│  运行环境      │ 浏览器 WASM            │ 系统级                   │     │
│  性能          │ 慢 (WASM)              │ 快 (本地)                │     │
│  系统调用      │ 无                     │ 有限支持                │     │
│  网络          │ 无                     │ 可配置                  │     │
│                                                                             │
│  Pyodide 优势:                                                           │
│  • 浏览器直接运行                                                         │
│  • 无需安装                                                              │
│  • 纯前端方案                                                            │
│                                                                             │
│  OlaForge 优势:                                                          │
│  • 功能更全                                                              │
│  • 性能更好                                                              │
│  • 系统集成能力更强                                                       │
│  • 可运行任意代码                                                         │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 21.3 综合对比表

```
┌────────────────────────────────────────────────────────────────────────────┐
│                           综合对比矩阵                                     │
├──────────────┬─────────┬─────────┬─────────┬─────────┬─────────┬─────────┤
│   维度       │  E2B   │Codespace│ Docker │ Pyodide│SkillLite│OlaForge │
├──────────────┼─────────┼─────────┼─────────┼─────────┼─────────┼─────────┤
│ 本地免费     │   ❌    │   ❌    │   ✅    │   ✅    │   ✅    │   ✅    │
│ 开源        │   ❌    │   ❌    │   ✅    │   ✅    │   ✅    │   ✅    │
│ 零依赖      │   ❌    │   ❌    │   ❌    │   ✅    │   ✅    │   ✅    │
│ 快速启动    │   ⚠️    │   ❌    │   ❌    │   ⚠️    │   ✅    │   ✅    │
│ 智能进化    │   ❌    │   ❌    │   ❌    │   ❌    │   ✅    │   ✅    │
│ 硬件隔离    │   ✅    │   ✅    │   ❌    │   ❌    │   ❌    │   ✅    │
│ 多语言     │   ✅    │   ✅    │   ✅    │   ❌    │   ✅    │   ✅    │
│ 可扩展     │   ⚠️    │   ⚠️    │   ✅    │   ⚠️    │   ✅    │   ✅    │
│ 企业支持    │   ✅    │   ✅    │   ✅    │   ❌    │   ❌    │   ✅    │
├──────────────┼─────────┼─────────┼─────────┼─────────┼─────────┼─────────┤
│ 推荐场景    │ 云服务  │ 云开发  │ 容器   │ 前端   │ 本地AI │ 全场景 │
│             │ 企业    │ 个人    │ 部署   │ 实验   │ 开发   │        │
└──────────────┴─────────┴─────────┴─────────┴─────────┴─────────┴─────────┘

✅ 优秀  ⚠️ 一般  ❌ 不足
```

---

### 21.4 各场景推荐

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            场景推荐                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  场景1: 个人学习/实验                                                      │
│  推荐: OlaForge (免费、本地、零依赖)                                       │
│  备选: Pyodide (纯浏览器)                                                 │
│                                                                             │
│  场景2: 生产环境代码执行                                                  │
│  推荐: OlaForge + KVM (安全、隔离)                                        │
│  备选: E2B / Docker                                                       │
│                                                                             │
│  场景3: 云端开发环境                                                      │
│  推荐: GitHub Codespaces                                                  │
│  备选: AWS CloudShell                                                     │
│                                                                             │
│  场景4: 团队协作平台                                                      │
│  推荐: OlaForge 企业版 + Claude Code                                      │
│  备选: Cursor + Docker                                                    │
│                                                                             │
│  场景5: AI Agent 安全执行                                                 │
│  推荐: OlaForge (本地) + Claude Code                                      │
│  备选: E2B SDK                                                            │
│                                                                             │
│  场景6: 嵌入式/离线环境                                                   │
│  推荐: OlaForge (零依赖)                                                  │
│  备选: Docker (需要容器运行时)                                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 21.5 OlaForge 竞争优势

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          竞争优势总结                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1️⃣  唯一"本地免费 + 开源 + 智能化"方案                                   │
│      • E2B/CodeSandbox: 收费闭源                                          │
│      • SkillLite/CubeSandbox: 单一能力                                    │
│                                                                             │
│  2️⃣  渐进式复杂度设计                                                     │
│      • 个人: 零依赖即开即用                                               │
│      • 团队: 一键部署                                                     │
│      • 企业: K8s 集群                                                     │
│                                                                             │
│  3️⃣  智能+安全双重核心                                                    │
│      • 安全: L0-L3 分级隔离                                               │
│      • 智能: 自动进化能力                                                 │
│                                                                             │
│  4️⃣  生态兼容性                                                           │
│      • MCP 协议                                                           │
│      • Claude Code 集成                                                   │
│      • 开发者熟悉度                                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 21.6 风险与挑战

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            风险与挑战                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  风险1: 生态竞争                                                          │
│  对手: E2B, Cursor, Docker 已有成熟生态                                   │
│  对策: 差异化定位，本地免费切入点                                          │
│                                                                             │
│  风险2: 技术复杂度                                                        │
│  对手: KVM 集成复杂度高                                                   │
│  对策: 本地模式使用 bwrap，企业版才用 KVM                                 │
│                                                                             │
│  风险3: 社区建设                                                          │
│  对手: 开源社区需要时间                                                  │
│  对策: 文档优先，降低参与门槛                                             │
│                                                                             │
│  风险4: 商业化路径                                                        │
│  对手: 免费 vs 收费平衡                                                  │
│  对策: 企业版 + 服务支持                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

*文档创建时间: 2026-04-29*
*最后更新: 2026-04-29*
*版本: v1.3*
*最后更新: 2026-04-29*
*版本: v1.1*
*版本: v1.0*